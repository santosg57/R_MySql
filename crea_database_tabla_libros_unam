#!/bin/bash

mysql -h localhost -u root -pcarlos12 << EOF
use Libros; 
describe Persons;
INSERT INTO Persons VALUES (123, 'Leopoldo', 'Gonzalez', 'lomas del Marques', 'Queretaro');
SELECT * FROM Persons > ss;
EOF
#show databases;
quit
